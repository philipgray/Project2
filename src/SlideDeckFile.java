
/**
 * Class to manage saving and loading files. Saves and loads files as JSONs
 * 
 * @author Alex Wills
 * @date 5 March 2022
 */
public class SlideDeckFile {
    


    /**
     * Loads a .json file to create a slide deck
     * 
     * @param filePath the path of the file to load
     * @return the SlideDeck specified by the file
     */
    public static SlideDeck loadFile(String filePath){
        SlideDeck deck = new SlideDeck();


        
        return deck;
    }
}
